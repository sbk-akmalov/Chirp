import SideBar from "@/components/layout/SideBar";

const Notifications = () => {
  return (
    <div className="h-full flex">
      <div className="flex-grow max-w-[600px] border-x px-4">Notifications</div>
      <SideBar />
    </div>
  );
};

export default Notifications;
