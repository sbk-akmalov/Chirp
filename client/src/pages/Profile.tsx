import SideBar from "@/components/layout/SideBar";

const Profile = () => {
  return (
    <div className="h-full flex">
      <div className="flex-grow max-w-[600px] border-x px-4">Profile</div>
      <SideBar />
    </div>
  );
};

export default Profile;
