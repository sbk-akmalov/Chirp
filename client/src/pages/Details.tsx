import SideBar from "@/components/layout/SideBar";

const Details = () => {
  return (
    <div className="h-full flex">
      <div className="flex-grow max-w-[600px] border-x px-4">Details</div>
      <SideBar />
    </div>
  );
};

export default Details;
